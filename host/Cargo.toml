[package]
name = "host"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "host"
path = "src/main.rs"

[dependencies]
sha3 = "=0.10.8"
anyhow = "=1.0.98"
clap = { version = "=4.5.37", features = ["derive"] }

[dev-dependencies]
# integration tests connect to a running simulator instance
